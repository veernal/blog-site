spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        # User Service Routes
        - id: user-registration
          uri: lb://user-service
          predicates:
            - Path=/api/v1.0/blogsite/user/register
            - Method=POST
        
        - id: user-login
          uri: lb://user-service
          predicates:
            - Path=/api/v1.0/blogsite/user/login
            - Method=POST
        
        # Blog Command Service Routes (Write Operations)
        - id: blog-add
          uri: lb://blog-command-service
          predicates:
            - Path=/api/v1.0/blogsite/user/blogs/add/**
            - Method=POST
          filters:
            - AuthenticationFilter
        
        - id: blog-delete
          uri: lb://blog-command-service
          predicates:
            - Path=/api/v1.0/blogsite/user/delete/**
            - Method=DELETE
          filters:
            - AuthenticationFilter
        
        # Blog Query Service Routes (Read Operations)
        - id: blog-by-category
          uri: lb://blog-query-service
          predicates:
            - Path=/api/v1.0/blogsite/blogs/info/**
            - Method=GET
        
        - id: blog-by-user
          uri: lb://blog-query-service
          predicates:
            - Path=/api/v1.0/blogsite/user/getall
            - Method=GET
          filters:
            - AuthenticationFilter
        
        - id: blog-by-category-duration
          uri: lb://blog-query-service
          predicates:
            - Path=/api/v1.0/blogsite/blogs/get/**
            - Method=GET
        
        # Administrative sync endpoint (for local development)
        - id: blog-admin-sync
          uri: lb://blog-query-service
          predicates:
            - Path=/api/v1.0/blogsite/admin/**
            - Method=POST
        
        # Swagger/OpenAPI Routes for Each Service
        - id: user-service-swagger
          uri: lb://user-service
          predicates:
            - Path=/swagger/user-service/**
          filters:
            - RewritePath=/swagger/user-service/(?<segment>.*), /${segment}
        
        - id: blog-command-service-swagger
          uri: lb://blog-command-service
          predicates:
            - Path=/swagger/blog-command-service/**
          filters:
            - RewritePath=/swagger/blog-command-service/(?<segment>.*), /${segment}
        
        - id: blog-query-service-swagger
          uri: lb://blog-query-service
          predicates:
            - Path=/swagger/blog-query-service/**
          filters:
            - RewritePath=/swagger/blog-query-service/(?<segment>.*), /${segment}

server:
  port: 9090
  ssl:
    enabled: false
    # Uncomment for SSL in production
    # key-store: classpath:keystore.p12
    # key-store-password: changeit
    # key-store-type: PKCS12
    # key-alias: blogsite

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,gateway,swagger-ui,openapi
  endpoint:
    health:
      show-details: always
    gateway:
      enabled: true

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    reactor.netty: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

jwt:
  secret: ${JWT_SECRET:blogsite-secret-key-for-jwt-token-generation-and-validation-2026}
  expiration: 86400000 # 24 hours

# Rate Limiting Configuration
redis:
  host: localhost
  port: 6379

springdoc:
  swagger-ui:
    urls:
      - url: /swagger/user-service/v3/api-docs
        name: User Service
      - url: /swagger/blog-command-service/v3/api-docs
        name: Blog Command Service
      - url: /swagger/blog-query-service/v3/api-docs
        name: Blog Query Service
